<% if @restaurants.any? %>
  <% @restaurants.each do |rest| %>
    <h2><%= rest.name %></h2>
  <% end %>
<% else %>
  <h2> no restaurants added yet</h2>
<% end %>
<%= link_to('Add a restaurant', {action: 'new'})%>
